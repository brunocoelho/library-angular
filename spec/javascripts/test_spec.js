describe('Login page', function(){
  it('should not redirect to books page due wrong credentials', function(){
    expect(true).toBe(true);
    expect(true).toBe(false);
    //input('user[email]').enter('bruno@gmail.com');
    //input('user[password]').enter('12345678');
    //elemento('input commit').click();
    //expect(browser().location().url()).toBe('/');
  });

  xit('should redirect to books pages due right credentials', function(){
  });
});
